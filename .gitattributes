# Normalize line endings to LF in the repo (we run on Linux containers / arm64).
* text=auto eol=lf

# Binary fixtures must not be touched.
*.jpeg binary
*.jpg binary
*.png binary
*.heic binary
*.webp binary
*.ico binary
