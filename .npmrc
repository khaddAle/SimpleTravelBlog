# Exact pins only — never write ^/~ ranges (see CLAUDE.md / npm-safe-install skill).
save-exact=true
save-prefix=
# Never run package lifecycle scripts on install (supply-chain mitigation).
ignore-scripts=true
